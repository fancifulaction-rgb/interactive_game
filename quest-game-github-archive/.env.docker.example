# Минимум для docker compose build (ключи вшиваются в статику на этапе сборки).
# Скопируйте: cp .env.docker.example .env

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=

# Порт хоста для UI (внутри контейнера nginx слушает 80)
QUEST_GAME_PORT=8080
