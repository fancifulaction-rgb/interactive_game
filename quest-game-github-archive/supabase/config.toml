# Минимальный конфиг для `npx supabase functions deploy`
# project_id: Dashboard → Settings → General → Reference ID

project_id = "tvytsnnujaucoluoyvjq"

[api]
enabled = true

# verify_jwt — только per-function, не под [functions]
[functions.player-upload]
verify_jwt = false

[functions.delete-game]
verify_jwt = false
